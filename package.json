{
  "name": "the-system-awakening",
  "version": "0.1.0",
  "description": "The System — Awakening: Real-life RPG platform",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace @tsa/web",
    "dev:api": "npm run dev --workspace @tsa/api",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && npm run test:api",
    "test:api": "apps/api/.venv/bin/python -m pytest apps/api/tests --cov=apps/api/app --cov-report=term --cov-fail-under=80 -q",
    "test:e2e": "playwright test",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present && apps/api/.venv/bin/ruff check apps/api",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run test:e2e"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
